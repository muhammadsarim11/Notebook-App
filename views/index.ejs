<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
   <div class="main w-full min-h-screen bg-zinc-900">
    <div class="form  p-10 text-white">
        <form action="/create" method="post">

            <input class="block px-2 py-3 w-full bg-zinc-800 rounded-md outline-none" name="title" type="text"  placeholder="Title goes here">
            <textarea class="block resize-none px-2 py-3 mt-2 bg-zinc-800 w-full rounded-md outline-none" name="details" id="" placeholder="Write your notes here"></textarea>
            <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create notes">
        </form>
    </div>
    <div class="tasks p-10 flex gap-4 flex-wrap">
        <% if(files.length>0){%>
            <% files.forEach(function(val){%>
        
                <div class="task  w-72 px-3 py-4 rounded-md bg-zinc-800">
              
             
                    <h1 class="text-white text-3xl tracking-lighter"><%=val%></h1>
                    <div class="flex w-full justify-between items-center mt-4">
                        <a href="/file/<%= val %> " class="inline-block text-blue-600">read more</a>
                        <a class="text-zinc-400" href="/edit/<%= val%>">edit filename</a>
                    </div>
            
                </div>

                <%}) %>

       <% } else{%>
        <h3 class="text-zinc-600 text-3xl">No Tasks Yet...</h3>

      <% }%>


    </div>
   </div>
</body>
<script src="/javascript/script.js"></script>
</html>